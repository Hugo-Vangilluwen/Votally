[package]
name = "libvotally"
version = "0.1.0"
edition = "2026"

[dependencies]
rand = "0.10.1"
ron = "0.12.0"
serde = { version = "1.0.228", features = ["derive"] }
tokio = { version = "1", features = ["net", "sync", "io-util", "rt", "macros"] }
